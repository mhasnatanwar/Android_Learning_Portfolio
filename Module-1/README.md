# Module 1 - Android Basics

## Completed pathways

1. Introduction to Kotlin
2. Set up Android Studio
3. Build a basic layout

The [completion screenshot](Screenshots/module-1-completion.png) shows all three pathways at 100%. The representative exercise in `Source-Code` is a small profile card built with composable functions, a `Column`, an icon, and styled text.

## Implementation feature

The screen separates the reusable `ProfileCard` composable from the activity. This demonstrates basic Compose layout, modifiers, spacing, typography, and a preview without adding unnecessary architecture to a static screen.

## Evidence still required

- Add an emulator/device screenshot of the running app to `Screenshots/`.
- Add authentic badge-achievement screenshot(s) to `Badge-Evidence/`.
- Complete `Analysis.md` in your own words.

## Run

Open `Source-Code` in Android Studio, allow Gradle sync to finish, and run the `app` configuration on an emulator or Android device.

